[package]
name = "usvg"
version = "0.46.0"
keywords = ["svg"]
license.workspace = true
edition = "2024"
rust-version = "1.87.0"
description = "An SVG simplification library."
categories = ["multimedia::images"]
repository = "https://github.com/linebender/resvg"
documentation = "https://docs.rs/usvg/"
readme = "README.md"
exclude = ["tests"]
workspace = "../.."

[[bin]]
name = "usvg"
required-features = ["text", "system-fonts", "memmap-fonts"]

[dependencies]
base64 = "0.22" # for embedded images
log = "0.4"
pico-args = { version = "0.5", features = ["eq-separator"] }
strict-num = "0.1.1"
svgtypes = "0.16.1"
tiny-skia-path = "0.12.0"
xmlwriter = "0.1"

# parser
data-url = "0.3" # for href parsing
flate2 = { version = "1.1", default-features = false, features = ["rust_backend"] } # SVGZ decoding
imagesize = "0.14.0" # raster images size detection
kurbo = "0.13.0" # Bezier curves utils
roxmltree = "0.21.1"
simplecss = "0.2"
siphasher = "1.0" # perfect hash implementation

# text
fontdb = { version = "0.23.0", default-features = false, optional = true }
fontique = { git = "https://github.com/linebender/parley", rev = "571ef7d3c329e466f60f131b7188277472c54177", default-features = false, features = ["std"], optional = true }
text_primitives = { git = "https://github.com/linebender/parley", rev = "571ef7d3c329e466f60f131b7188277472c54177", default-features = false, features = ["std"], optional = true }
harfrust = { version = "0.5.0", optional = true }
skrifa = { version = "0.40", optional = true, default-features = false, features = ["std"] }
unicode-bidi = { version = "0.3", optional = true }
unicode-script = { version = "0.5", optional = true }
unicode-vo = { version = "0.1", optional = true }

[dev-dependencies]
once_cell = "1.21"

[features]
default = ["text", "system-fonts", "memmap-fonts"]
# Enables text-to-path conversion support.
# Adds around 400KiB to your binary.
text = ["fontdb", "fontique", "text_primitives", "harfrust", "skrifa", "unicode-bidi", "unicode-script", "unicode-vo"]
# Enables system fonts loading.
system-fonts = ["fontdb/fs", "fontdb/fontconfig", "fontique/system"]
# Enables font files memmaping for faster loading.
memmap-fonts = ["fontdb/memmap"]
